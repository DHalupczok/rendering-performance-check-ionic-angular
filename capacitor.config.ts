import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rendering-performance-check-ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
